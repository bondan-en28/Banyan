<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bintang.banyan"
    android:targetSandboxVersion="1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Activity.WelcomeScreen.WelcomeScreenActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activity.DetailPost.DetailPostActivity" />
        <activity android:name=".Activity.AddPost.AddPostActivity" />
        <activity android:name=".Activity.AboutActivity" />
        <activity
            android:name=".Activity.RegisterActivity"
            android:theme="@style/AppThemePastel" />
        <activity
            android:name=".Activity.LoginActivity"
            android:theme="@style/AppThemePastel" />
        <activity android:name=".Activity.SplashActivity" />
        <activity
            android:name=".Activity.Main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppThemeLightStatusBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />
    </application>

</manifest>